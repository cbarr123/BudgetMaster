@model IEnumerable<BudgetMaster.Models.ProjectedExpense>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BudgetId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExpenseCategoryId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Amount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BudgetId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExpenseCategoryId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProjectedExpenseId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProjectedExpenseId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProjectedExpenseId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
